This folder contains all scripts necessary to create the initial cluster.

To start a new cluster:
1. `cp .env.example .env` and fill in variables.
2. Run `./create.sh`. May need to run twice.
3. You're done!
